/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      './store/templates/store/*.html',
      './store/templates/store/**/*.html',
      // Add paths to other apps if necessary
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
  